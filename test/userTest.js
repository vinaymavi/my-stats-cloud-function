/**
 * User tests
 */
const chai = require("chai");
const assert = chai.assert;
describe("User", ()=> {
    it("Should be empty", ()=> {
        assert.equal(1, 1);
    });

    it("Insert", ()=> {
        assert.equal(1, 1);
    });
    it("Find By Fb Id", ()=> {
        assert.equal(1, 1);
    });

    it("Find By Device Id", ()=> {
        assert.equal(1, 1);
    });
    it("Should be valid", ()=> {
        assert.equal(1, 1);
    });
    it("Should be invalid", ()=> {
        assert.equal(1, 1);
    });
});
